version: '3'

services:
  coremvc:
    image: coremvc
    build:
      context: ./CoreMVC
      dockerfile: Dockerfile
    depends_on: 
      - corewebapi
    ports:
      - "80:80"
      
  corewebapi:
    image: corewebapi
    build:
      context: ./CoreWebAPI
      dockerfile: Dockerfile
    ports:
      - "8080:80"

  
