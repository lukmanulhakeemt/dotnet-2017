version: '3.1'

services:
  viz:
    image: portainer/portainer
    volumes: 
      - "/var/run/docker.sock:/var/run/docker.sock"
    ports:
      - "9000:9000"
    deploy:
      placement:
        constraints:
          - node.role==manager 